name: Ping Supabase

on:
  schedule:
    - cron: '0 8 * * 1'  # Lunes a las 8:00 UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar a Supabase
        run: |
          curl -I https://gxtlctdrpftzqoqrhfkz.supabase.co

